/**
 * 引用组件隐射表
 * 映射名，路径
 * 引用组件相同，但路径不会相同，用路径做唯一索引
 */


module.exports = {
  input: {
    tagName: 'customInput',
    path: "@/compoments/customInput"
  },
  select: {
    tagName: 'customSelect',
    path: "@/compoments/customInput"
  }
}